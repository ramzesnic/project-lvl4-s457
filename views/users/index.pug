extends /layouts/application.pug

block content
  table.table.table-stripped.table-hover.mt-4
    thead
      tr
        th Full Name
        th Email
        th Created At
    tbody
      each user in users
        tr
          td= user.fullName
          td= user.email
          td= user.formatedDate