doctype html
html(lang="en")
  head
    title= Title
    script(src='/assets/main.js')
  body
    nav.navbar.navbar-expand-lg.navbar-light.bg-light
      a.navbar-brand(href="/") Task Manager
      button.navbar-toggler(data-toggle="collapse" data-target="#navbarSupportedContent")
        span.navbar-toggler-icon
      .collapse.navbar-collapse
        ul.navbar-nav.mr-auto
          li.nav-item.active
            a.nav-link(href='#') Users
        ul.navbar-nav
          if isSignedIn()
            li.nav-item
              a.nav-link(href='#' data-method='delete' rel='nofollow') Sign Out
          else
            li.nav-item
              a.nav-link(href='#') Sign In
            li.nav-item
              a.nav-link(href='#') Sign Up
    .container
      - const msg = flash.get()
      if msg
        .alert.alert-info.m-3= msg
      block content